server:
  port: 8085

spring:
  application:
    name: anax-kogito-sample
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

kogito:
  workflow:
    version-strategy: workflow

anax:
  metadata-server:
    url: ${METADATA_SERVER_URL:http://metadata-platform:3001}
